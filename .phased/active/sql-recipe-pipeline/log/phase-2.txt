⚠ Phase 2 has issues: 4 of the 10 contract tests carry premises no implementation can satisfy.

The grammar itself is built and green — four mixins (`DbElements`, `SchemaElements`, `TableElements`, `ColumnElements`), typed `Literal` signatures, `_meta` projection flags, out-of-scope elements removed, `tests/test_grammar.py` rewritten to the new vocabulary: 31 passed, `ruff check src tests` clean. The 4 failures are plan defects recorded in the `[!]` note with the exact edits repair needs: three tests read the tree with `Bag.__getitem__` (value semantics) where they need `Bag.get_node`, and `test_relation_cardinality_at_most_one` expects `validate_source()` to report a max-cardinality violation that genro-builders raises at insertion instead. Committed as `b29aff8`; foreman unreachable from this sub-session, so the notification was skipped.
