✓ Phase 6 completed: SqlModelReader — normalized JSON to source tree

Contract tests 11/11 green, full suite 71 passed (exit 0), `ruff check src tests` clean; commit `471c5a5`. Two plan decisions had to bend and are recorded in `notes.md` and the plan's `Done:` — D3's automatic FK index made the round-trip law unsatisfiable, so `relation` gained `indexed: bool = True` (default preserves D3 and the Phase 4 golden), and hashed *index* names travel verbatim because `index.name` is both the collection key and the echoed physical `index_name`. D4's composite FK target was made reachable end-to-end (renderer and validator expand a three-part `to` naming a compositeColumn) and is covered by a new test in `test_grammar.py`; the foreman notification was skipped — no desktop messaging channel exists in this sub-session.
