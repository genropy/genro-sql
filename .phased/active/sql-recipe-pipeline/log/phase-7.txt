⚠ Phase 7 has issues: the contract's `'name="idx_' not in source_code` assertion is unsatisfiable — filed as a plan-defect claim.

`SqlPythonEmitter` is implemented and 4 of the 5 contract tests pass, including the live-PostgreSQL closure (apply → introspect → read → emit → exec → render → diff empty); full suite 75 passed / 1 failed, `ruff check src tests` clean. The one failure is provable, not a bug: Phase 4's golden fixes an index's recipe `name` as the physical `index_name`, `table`'s `collection_key="name"` makes an anonymous index unbuildable, and Phase 6's round-trip requires an unnamed source index to return with its hashed `index_name` intact — so a recipe rebuilding it must carry `name="idx_<hash>"`. The plan entry names the exact one-line contract edit (drop `"idx_"` from the prefix tuple) and the alternative grammar route, which would reopen Phase 4 and is not this phase's call; `tests/conftest.py` also gained a pg facade that adopts the migrated recipe's label and schemas, since the Phase 5 hardcoding served exactly one recipe.
