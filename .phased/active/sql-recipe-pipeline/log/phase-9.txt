✓ Phase 9 completed: Coherence review and auto-fix (final, mandatory) — commit `6931e36`.

`ruff check .` clean repo-wide, `pytest -q` 80 passed (4 postgresql-marked included, verified with `-m postgresql`). Auto-fixed dead `key` parameters in validators/reader, `_plane(name, …)`, the ten missing `# wf:phase-8:new` markers in `grammar_doc.py`, and the ruff glob that left the plan's own contract copies failing a repo-wide lint; six items went to `review.md` for a human, the largest being the private `_class_schema_to_grammar_document` import from genro-builders (no public in-memory route exists) and the triplicated `_names()` whose reader copy skips `.strip()`. The plan is now fully `[x]`; two readability checks deferred by Phases 7 and 8 are waiting in `verify.md`, and the foreman notification silently skipped (no desktop messaging in this sub-session).
