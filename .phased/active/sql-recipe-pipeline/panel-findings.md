# Panel pre-commit review — findings (quality check interrupted before the stamp)

Run: 16 agents — 4 dimension reviewers (correctness, cross-phase, pattern, coverage)
over afc470e..HEAD, 34 raw findings; the 4 highest-severity each verified by 3
independent skeptics prompted to refute (majority rules). The remaining 30 were
NOT judged (agent budget) — plausible, neither confirmed nor dismissed.

STATUS: the user ran out of credits before answering the fix-first-or-stamp
question. No stamp written; nothing fixed. QA pass: done (both readability
checks passed). Naming review: done, markers stripped (commit 2948f49).

## Confirmed (4/4 survived the skeptics — all in the reverse path, reader.py)

### [high] Reader discards the foreign key's constraint_name, so every real database's FKs get renamed on the next migration
- Where: `src/genro_sql/modern/reader.py:263` — dimension: correctness — skeptic votes: SSR (S=survives)
- Claim: Read a database that has named FK constraints (every introspected one does: `readers/base_reader.py:243` pops `constraint_name` off the entity and passes it to `new_relation_item`), emit the recipe, re-render it, and the FK comes back with `constraint_name = 'fk_<md5>'`. The migrator's `changed_relation` sees a `constraint_name` change and emits `RENAME CONSTRAINT recipe_author_id_fkey TO fk_7e4d9a12;` (or, with `ignore_constraint_name`, silently reverts it) for every foreign key in the schema. The module's stated law `render(reader(J)) == J` is false for any J whose relations are named.
- Evidence: `_fill_relations` builds kwargs from `to`, `on_delete`, `on_update`, `deferred`, `indexed` only — `constraint_name` is in `_RELATION_KEYS` (line 50-54) so `_check_keys` accepts it, and then nothing reads it. The `relation` element (elements.py:294) has no parameter that could carry it, so the name is unrepresentable, not merely unread. The docstring justifies this with "structural hashes are not recipe names, where a name is optional" — but an introspected FK name is not a hash. Untested: `tests/test_wf_phase6_reader.py::test_single_column_fk_with_actions_and_deferrable` builds its fixture through `JsonStructureProducer` without a relation `name`, and `JsonStructureProducer` does support one (`json_producer.py:141`, `constraint_name=relation.get("name")`). The phase-5 PG integration recipes contain no FK at all.
  - HOLDS: Tried to break it three ways; the mechanical core survives, only the blast radius is overstated.

Holds (verified read-only this session):
- src/genro_sql/modern/reader.py `_fill_relations` builds kwargs from `to`, `on_delete`/`on_update`, `deferred`, `indexed` only. `constraint_name` sits in `_RELATION_KEYS` (lines 50-54) so `_check_keys` accepts it, and nothing else in the reader reads it (grep for constraint_name in modern/ hits only the CONSTRAINT paths at reader.py:233, 289-297 and migration.py:229/235).
- Unrepresentable, not merely unread: `relation()` at elements.py:294-324 has no name parameter; `**extra` carries semantic/x_ attrs the renderer never projects as constraint_name.
- The value really exists on introspected input: genro-sqlmigration readers/base_reader.py:243 pops constraint_name into `new_relation_item`, and structures.py:305-306 always stores it, defaulting to `hashed_name(..., obj_type='fk')` = `fk_<8 hex>`. So recipe-originated JSON round-trips (name == hash), introspected JSON (`recipe_author_id_fkey`) does not.
- `json_equal` (structures.py:384-400) is a strict sorted-key json.dumps compare, so the docstring law `render(reader(J)) == J` (reader.py:11-13) is genuinely false for named FKs — exactly the use case that docstring advertises ("a database that only exists as introspected JSON becomes a source tree").
- Untested confirmed: tests/test_wf_phase6_reader.py:83 authors its relation with no `name`, while JsonStructureProducer would honour one (json_producer.py:141). No test or doc mentions relation constraint_name.
- Side effect: this also punctures review.md item 4's reasoning ("nothing unrepresentable ever reaches the tree, _check_keys raises") — constraint_name is inside structure-1.0, passes _check_keys, and is dropped silently. Different finding, so no duplication.

Refuted part — the headline consequence. `ignore_constraint_name` defaults to True (migrator.py:137; cli.py:27 sets it true), and in that mode command_builder.py:760-782 `changed_relation` does `relattr['constraint_name'] = oldvalue; return` — no DDL, the database keeps `recipe_author_id_fkey`. So "every real database's FKs get renamed on the next migration" and the `RENAME CONSTRAINT` line only occur with a non-default `ignore_constraint_name=False`; the default revert is the migrator's intended no-op, not damage.

Also weighing against novelty (not enough to refute): the loss is declared — the reader docstring says relations come back anonymous, and plan.md:315 states "relations are anonymous". The actual defect is the mismatch with the unqualified round-trip law three lines above, plus the asymmetry with UNIQUE/CHECK, whose readable names ARE preserved (reader.py:289-299).

Net: code-level claim stands (FK name dropped, unrepresentable, untested, stated law false for introspected input); the consequence should be downgraded to recipe/JSON drift under default configuration, with RENAME churn only for callers who disable ignore_constraint_name.
  - HOLDS: Tried to refute and could not — every link checks out, though the headline severity is overstated. Verified: reader.py:255-268 (_fill_relations) builds kwargs from to/foreign_key/on_delete/on_update/deferred/indexed only; constraint_name sits in _RELATION_KEYS (50-54) so _check_keys (320) accepts the key and never looks at the value — silent drop, no hash check. The constraint path at 289-299 does the opposite (`elif constraint_name != name:` keeps a readable name), so the reader's own rule (hash -> drop, readable -> keep) is applied everywhere except relations. No channel back: elements.py:294 relation() has no name parameter, and migration.py:187-201 never passes constraint_name to new_relation_item, so structures.py:305 (`constraint_name or hashed_entity_name`) always substitutes fk_<hash>. Named FKs are real input: base_reader.py:243 forwards the introspected name, json_producer.py:141 forwards relation["name"]. The law is strict: json_equal is json.dumps(sort_keys=True) string equality, so render(reader(J)) != J for any J with a non-hash relation constraint_name — and the reader's docstring names introspected databases as the intended input. Coverage gap confirmed: test_wf_phase6_reader.py fixtures pass no relation name, and test_wf_phase7_emitter.py::test_pg_end_to_end_closure's FK was created BY the migrator from a nameless recipe, so PG holds it under the hash and the round-trip preserves the name by accident; no test uses a pre-existing differently-named FK. Two corrections to the framing, neither refuting: (1) the RENAME half needs the non-default flag — migrator.py:137 defaults ignore_constraint_name=True and command_builder.py:778-785 then just restores oldvalue and returns without setting a command, and executor.getChanges() collects only commands, so with defaults the SQL is empty and the real damage is silent loss of the DB's FK names in the emitted recipe; the per-FK `RENAME CONSTRAINT` appears only with ignore_constraint_name=False. (2) "unrepresentable" holds end-to-end but not at the signature: relation(**extra) would accept the attribute; it is the renderer that has no path for it. plan.md:315 does say "relations are anonymous", so it is partly planned — but its justification is about structural hashes while the same line preserves readable constraint names, so the relation exception is unmotivated and does break the module's stated law for any introspected database whose FKs are named.
  - REFUTES: The code facts are right but the harm is not. Three things break the finding.

1) The claimed DDL never fires under the shipped configuration. `SqlMigrator.__init__` takes `ignore_constraint_name=True` by default (/Users/fporcari/Development-kyo/bricks/genro-sqlmigration/src/genro_sqlmigration/migrator.py:137, and cli.py:27 writes `true` into the generated config). In that branch `changed_relation` (command_builder.py:778-781) does `relattr['constraint_name'] = oldvalue; return` — it restores the database's own name and emits no command at all. So "every foreign key gets RENAME CONSTRAINT" is false: the rename requires opting into `ignore_constraint_name=False`, and at that setting a rename is the flag's requested behaviour for constraints and indexes alike, not a reader defect. Nor is it a "silent revert" of anything physical: the revert lands on the in-memory item, the FK in the database is untouched and no SQL is produced. That module's own docstring (command_builder.py:74-77, migrator.py:62-64) says the flag exists precisely because hash-named model constraints differ from hand-named DB ones — the ecosystem's designed answer to this situation.

2) It is a ratified plan decision, not an oversight. .phased/active/sql-recipe-pipeline/plan.md:315 decides for Phase 6: "Hashed entity names ... are NEVER used as recipe names: relations are anonymous, hashed UNIQUE constraints ... keep `constraint` with their readable `constraint_name` only when it differs from the hash". The asymmetry the finding points at (reader.py:295 preserves a non-hash UNIQUE name, `_fill_relations` preserves nothing) is exactly the shape the plan asked for, repeated in the closure note at plan.md:298. The finding is right that `relation` has no `name` parameter (elements.py:294-296) and that `_fill_relations` (reader.py:249-273) builds kwargs from `to`/actions/`deferred`/`indexed` only — but that makes it a grammar feature request (a new relation parameter plus a renderer path, since migration.py:186-201 builds the FK attribute dict from a fixed list and never passes `constraint_name` to `new_relation_item`, which then hashes at structures.py:301-306), not a bug introduced by this diff.

3) The round-trip law is not silently violated. reader.py:19-23 states the exception in the sentence right after the law: "A relation comes back anonymous". The finding's real target is the justification clause — "structural hashes are not recipe names, where a name is optional" — which indeed does not describe an introspected `recipe_author_id_fkey`. That is a docstring that under-explains a declared limitation, a documentation nit, not a correctness defect.

What survives is worth one line in a limitations note: reading an introspected schema and re-emitting it normalizes FK constraint names to `fk_<hash>`, and the test gap is real (tests/test_wf_phase6_reader.py has no fixture with a named relation, while json_producer.py:141 supports one). But no wrong SQL is emitted at default settings, no schema is damaged, and the behaviour is the one the plan chose.

### [high] Reader turns DEFERRABLE INITIALLY IMMEDIATE into INITIALLY DEFERRED
- Where: `src/genro_sql/modern/reader.py:267` — dimension: correctness — skeptic votes: SSS (S=survives)
- Claim: An FK declared `DEFERRABLE INITIALLY IMMEDIATE` (deferrable=True, initially_deferred absent) round-trips into `deferrable=True, initially_deferred=True`. The migrator's `changed_relation` falls through to the rebuild branch and emits DROP CONSTRAINT + ADD CONSTRAINT ... DEFERRABLE INITIALLY DEFERRED — a silent change of transactional semantics produced by the round-trip itself. The mirror case (initially_deferred=True with deferrable absent) is accepted by `_check_keys` and then dropped entirely.
- Evidence: reader: `if attributes.get("deferrable"): kwargs["deferred"] = True` — a single grammar flag; `initially_deferred` is listed in `_RELATION_KEYS` (line 53) but never read. migration.py:196-198 then writes both keys unconditionally: `attributes["deferrable"] = True; attributes["initially_deferred"] = True`. So the two states DEFERRABLE/IMMEDIATE and DEFERRABLE/DEFERRED collapse onto one, and the reader silently conserves something it cannot express, against its own docstring ("this reader conserves nothing it cannot express"). The only test covers the case where both flags are already True.
  - HOLDS: Tried to break it on four fronts; every one held.

1. Reader code is as quoted. /Users/fporcari/Development-kyo/bricks/genro-sql/src/genro_sql/modern/reader.py:267-268 reads only `deferrable` and sets `deferred=True`; `initially_deferred` is in `_RELATION_KEYS` (line 53) and never read anywhere else in the file. `_check_keys` (line 321) is pure key-membership — no cross-field validation, so `deferrable`-without-`initially_deferred` and the mirror case both pass silently.

2. The two states are genuinely distinct in the input contract, so this is not "an input that cannot happen". genro-sqlmigration's `schemas/structure-1.0.json` (relation block, lines ~178-180) declares `deferrable: {const true}` and `initially_deferred: {const true}` as independent optional keys — nothing requires them together, so `{deferrable: true}` alone is a valid structure-1.0 relation. And it is actually produced: `json_producer.py:136-138` copies each key only when truthy, and `readers/pg_reader.py:139,359` derives them from `condeferrable`/`condeferred` — PostgreSQL's `DEFERRABLE INITIALLY IMMEDIATE` is condeferrable=t/condeferred=f, i.e. exactly `{deferrable: true}` with no `initially_deferred`.

3. The write-back side amplifies rather than compensates. /Users/fporcari/Development-kyo/bricks/genro-sql/src/genro_sql/modern/migration.py:196-198 emits both keys unconditionally from the single `deferred` attr, and the modern grammar has no second flag (`modern/elements.py:296,314`, `docs/grammar.md:262` — `deferred` documented as "INITIALLY DEFERRED"). So JSON A `{deferrable}` → grammar `deferred=True` → JSON B `{deferrable, initially_deferred}`.

4. The downstream consequence is real. `diff_engine.py:123-135` reports the appeared `initially_deferred` as a changed attribute; `command_builder.changed_relation` (lines 760-812) only short-circuits for `constraint_name`, so it falls through to the rebuild branch and emits DROP CONSTRAINT + `ADD ...` built with `initially_deferred=relattr.get('initially_deferred')`, which `writers/pg_writer.py:366-374` renders as `DEFERRABLE INITIALLY DEFERRED`. A constraint that was checked at statement end now defers to commit — a transactional-semantics change caused purely by the round-trip.

No decision covers it as intended loss: no `deferr` mention in plan.md's D1-D12 or elsewhere, and the plan's own Phase-6 Done gate (plan.md:319) asks for round-trip `json_equal(A,B)` on a fixture "with actions and deferrable" — the single fixture, tests/test_wf_phase6_reader.py:96, sets both flags True, which is the one input the collapse cannot break. Mirror case also confirmed: `{initially_deferred: true}` alone is accepted by `_check_keys` and then discarded entirely (not SQL-reachable from PG, but reachable from hand-written JSON, and the reader's docstring — "this reader conserves nothing it cannot express" — promises a loud error, not a silent drop).

Narrow in practice (deferrable-immediate FKs are uncommon), but the mechanism is exactly as described.
  - HOLDS: Tried to refute it and could not — every link in the chain checks out on the actual code.

1. Grammar side: `src/genro_sql/modern/elements.py:296,314` — `relation(..., deferred: bool = False)` documented as "INITIALLY DEFERRED". There is exactly one flag, so DEFERRABLE INITIALLY IMMEDIATE is genuinely inexpressible in the modern grammar.

2. Reader side: `src/genro_sql/modern/reader.py:267-268` — `if attributes.get("deferrable"): kwargs["deferred"] = True`. `initially_deferred` is in `_RELATION_KEYS` (line 53) so `_check_keys` accepts it, and it is read nowhere in the module (grep over src confirms only lines 53 and 267 mention it). Both `{deferrable}` and `{deferrable, initially_deferred}` map onto the same builder state; `{initially_deferred}` alone maps onto none.

3. Forward side: `src/genro_sql/modern/migration.py:196-198` writes both keys unconditionally from the single `deferred` attr. So `render(reader(A)) != A` for A carrying `deferrable` alone — a direct violation of the module docstring's stated law and of its "this reader conserves nothing it cannot express" claim (it should have raised `SqlModelReadError`, as it does for other inexpressible input).

4. The input state is reachable, not hypothetical — which was the most promising refutation and it fails. `genro-sqlmigration/src/genro_sqlmigration/readers/pg_reader.py:138-139,358-359` introspects `con.condeferrable` and `con.condeferred` independently, and `json_producer.py:136-137` (`for key in ("deferrable", "initially_deferred"): if relation.get(key)`) drops the falsy one. A Postgres FK declared DEFERRABLE INITIALLY IMMEDIATE therefore arrives as `attributes = {"deferrable": True}` with no `initially_deferred` — exactly the input the reader mistranslates, and exactly the "database that only exists as introspected JSON" the docstring names as the use case.

5. Migration consequence confirmed: `genro-sqlmigration/src/genro_sqlmigration/command_builder.py:760-812` — `changed_relation` only short-circuits for `changed_attribute == 'constraint_name'`; a changed `initially_deferred` falls through to the rebuild branch, emitting `struct_drop_constraint_sql` + `ADD ...` with `initially_deferred=relattr.get('initially_deferred')`, and `writers/pg_writer.py:366-374` renders ` INITIALLY DEFERRED`. Real semantic change: the constraint stops being checked at statement end.

6. Test coverage as claimed: `tests/test_wf_phase6_reader.py:83-96` is the only deferrable fixture and it sets `"deferrable": True, "initially_deferred": True` — the one case that happens to round-trip.

The mirror case (`initially_deferred` alone, silently dropped) is unreachable from Postgres introspection since condeferred implies condeferrable, so that half is contract-hygiene rather than a live bug — but the primary case is a live, reachable round-trip defect. Not among review.md's six items (no deferrable mention there).
  - HOLDS: Tried to refute it; every link of the chain holds in the actual code.

1. Reader (/Users/fporcari/Development-kyo/bricks/genro-sql/src/genro_sql/modern/reader.py, `_fill_relations`): the only deferral logic is `if attributes.get("deferrable"): kwargs["deferred"] = True`. `initially_deferred` is declared in `_RELATION_KEYS` (line 53) so it passes `_check_keys` (lines 321-329, which only rejects *undeclared* keys), but it is never read — grep shows the string appears in the file only at line 53. No guard requires the two flags to agree.

2. Renderer (/Users/fporcari/Development-kyo/bricks/genro-sql/src/genro_sql/modern/migration.py:196-198): `if node.get_attr("deferred"): attributes["deferrable"] = True; attributes["initially_deferred"] = True` — unconditional, both keys.

3. The lossy input state is genuinely produced upstream, not hypothetical: /Users/fporcari/Development-kyo/bricks/genro-sqlmigration/src/genro_sqlmigration/readers/pg_reader.py:138-139 reads `con.condeferrable` and `con.condeferred` as two independent columns, and json_producer.py:136-138 emits each key only when truthy (`for key in ("deferrable", "initially_deferred"): if relation.get(key): attrs[key] = True`). A plain `DEFERRABLE` FK — which in PostgreSQL defaults to INITIALLY IMMEDIATE, i.e. the common spelling, not an exotic one — introspects to `{deferrable: True}` alone.

4. Downstream effect is as claimed: command_builder.py:760-812 `changed_relation` short-circuits only for a `constraint_name` change; anything else falls through to DROP + `ADD ...`, passing `initially_deferred=relattr.get('initially_deferred')`, and pg_writer.py:366-374 appends " INITIALLY DEFERRED" when both are set.

5. It contradicts the module's own stated contract — reader.py docstring "this reader conserves nothing it cannot express" — and docs/grammar.md:262 documents `deferred` as INITIALLY DEFERRED only, so the grammar truly cannot express DEFERRABLE/IMMEDIATE and the reader should have raised instead of silently promoting it.

6. Mirror case confirmed: `initially_deferred: True` with `deferrable` absent passes `_check_keys` and is dropped with no error (unreachable from PG introspection, reachable from any hand-written or non-PG structure-1.0 JSON).

7. Test-coverage claim confirmed: the only test occurrence is tests/test_wf_phase6_reader.py:96, with both flags already True; the collapsing case is untested.

Not a duplicate of review.md's six flagged items (grammar_doc private import, `_names` triplication, `_INDEX_OPTIONS` duplication, D7 dead TODO, `print()` in `__main__` demos, empty renderer.py placeholder).

### [high] Composite names built by joining columns with '_' are ambiguous, and one FK silently overwrites another
- Where: `src/genro_sql/modern/reader.py:238` — dimension: correctness — skeptic votes: SSS (S=survives)
- Claim: A table with columns `a_b, c, a, b_c`, one FK on `(a_b, c)` and another on `(a, b_c)`, reads back with a single compositeColumn named `a_b_c`. `_need_composite`'s `setdefault` keeps the first tuple, so the second relation is mounted on a composite covering the wrong columns; re-rendering emits two relation items with the same structural hash (both over `a_b, c`) and the second overwrites the first in `item["relations"]`. Net effect: one foreign key is silently deleted and the other duplicated — `render(reader(J)) != J`, with no error anywhere.
- Evidence: `_need_composite`: `name = "_".join(columns)`; `planned = self._composites[key].setdefault(name, {"columns": columns, ...})` — the returned entry's `columns` is ignored by the caller and the name is the only key. `_fill_relations` then resolves purely by name: `table["composites"]["_".join(columns)]` (line 262). The same ambiguity reaches `_target` (line 282) and `_plan_constraint_composite`, where a UNIQUE over `(a_b, c)` would stamp `unique=True` onto whichever composite claimed the name first. The existing guard (`_add_table`, line 167) only catches composite-vs-column collisions, never composite-vs-composite.
  - HOLDS: Verified end-to-end in the code; every link in the chain holds. (1) reader.py:238 `_need_composite` keys purely on `"_".join(columns)` and `setdefault` drops the second tuple. (2) The only guard in `_add_table` is `if name in columns` — composite-vs-column, so the synthesized `a_b_c` collides with the other composite unnoticed. (3) `_fill_relations` (line 262) and `_target` (line 282) resolve by name only, so both FKs mount on the composite over `(a_b, c)`. (4) No error path fires: `SqlModelReader.to_builder` calls `model.create()` + `validate_model()` (SqlModelValidator), never `validate_source`; the installed `genro_builders.builder.base.create` is setup/main/compute_logic and its docstring states validate_source is "called, never implied", so `SqlBuilder.validate_source`'s "relation: at most one per column" report (builder.py:63-83) is never reached, and `SqlModelValidator._check_relation` only compares local vs target column counts (2 vs 2 here) so it passes. (5) The overwrite on re-render is confirmed: migration.py `_fill_relations` does `item["relations"][relation["entity_name"]] = relation` with no duplicate check, and `new_relation_item` (genro-sqlmigration/src/genro_sqlmigration/structures.py:280-313) hashes only (schema, table, columns, 'fk') — the target is not part of the hash — so both re-rendered relations, both carrying columns (a_b, c), produce the same entity_name and one is silently lost while the other keeps the wrong source columns. This breaks the module's own stated law `render(reader(J)) == J` with no error anywhere. Plan D4 (plan.md:35-40, 314) only promises a loud error for composite-vs-COLUMN collisions, so this is an uncovered gap rather than a ratified trade-off, and review.md does not report it. Only caveat: the trigger needs adversarial column names (`a_b, c` next to `a, b_c`), so real-world likelihood is low — a severity argument, not a refutation.
  - HOLDS: Every link in the chain checks out; the finding stands. (1) Collision real: _need_composite (src/genro_sql/modern/reader.py:236) uses name = "_".join(columns), so FK columns (a_b, c) and (a, b_c) both yield "a_b_c"; setdefault keeps the FIRST tuple and the caller _plan_relation_composites discards the returned entry, using only the name — so one planned composite over (a_b, c). (2) _add_table (reader.py:165-176) creates exactly one compositeColumn(name="a_b_c", columns="a_b,c"); its only guard is `if name in columns` (composite-vs-column), and a_b_c is not among a_b/c/a/b_c, so nothing fires. No composite-vs-composite check exists anywhere. (3) _fill_relations (reader.py:262) resolves the owner purely by name, table["composites"]["_".join(columns)], so both relations mount on the same node — and the tree keeps both: `relation` under compositeColumn is declared unbounded (see SqlBuilder.validate_source docstring, builder.py:63-83, which says over-full documents are BUILT and only reported there) and genro-builders/_grammar.py:287 auto-labels the second child (relation_0/relation_1) instead of raising or replacing. (4) Nothing validates it away: SqlModelReader.to_builder calls model.validate_model() → SqlModelValidator, not validate_source, and SqlModelValidator has no "at most one relation per column" check (validators.py:39-68); _check_relation only compares local vs target column COUNTS (2 vs 2 here). SqlMigrationRenderer.render likewise runs only SqlModelValidator (migration.py:76). (5) The overwrite is confirmed: migration.py:184-202 takes columns from the OWNER composite for both relations (both → [a_b, c]), and new_relation_item (bricks/genro-sqlmigration/src/genro_sqlmigration/structures.py:301) hashes entity_name from (schema, table, columns, 'fk') only — related_table/related_columns are not in the hash — and the assignment `item["relations"][relation["entity_name"]] = relation` has no setdefault, so the second wins. One FK vanishes, the other carries the wrong source columns, with no error raised. The only mitigation is likelihood: the collision needs a specific column-name shape on the same table (legal introspected input, but uncommon), which lowers severity, not correctness — the round-trip law stated in reader.py's own docstring is silently violated.
  - HOLDS: Every link in the chain checks out in the code, so I could not refute it.

1. `_need_composite` (src/genro_sql/modern/reader.py:236-242) keys the plan solely by `name = "_".join(columns)` with `setdefault`, so `(a_b, c)` and `(a, b_c)` both map to `a_b_c` and only the first tuple survives. `_add_table` (lines 164-178) creates one `compositeColumn(name="a_b_c", columns="a_b,c")`; its only guard is composite-vs-column (`if name in columns`), never composite-vs-composite, and `a_b_c` is not a column here, so nothing raises.

2. `_fill_relations` (lines 258-262) resolves the owner purely by name — `table["composites"]["_".join(columns)]` — so the second FK is mounted on a composite over the wrong columns, with no KeyError.

3. Nothing downstream catches it. `SqlModelReader.to_builder` calls only `model.validate_model()` (SqlModelValidator); it never calls `SqlBuilder.validate_source`, the one place reporting "relation: at most one per column" (src/genro_sql/modern/builder.py:63-83). `SqlModelValidator._check_relation` compares only local vs target column counts (2 vs 2 here), so it passes.

4. The re-render collision is real: `SqlMigrationRenderer._fill_relations` (src/genro_sql/modern/migration.py:174-202) derives `columns` from the owner composite's `columns` attr — `a_b, c` for both relations — and stores by `item["relations"][relation["entity_name"]]`. `new_relation_item` (bricks/genro-sqlmigration/src/genro_sqlmigration/structures.py:280-313) hashes `entity_name` from schema+table+columns+'fk' only; the target is NOT part of the hash. Both projected relations therefore carry the identical hash and the second silently overwrites the first: one FK vanishes, the other is emitted over the wrong columns. `render(reader(J)) != J`, silently.

5. The secondary reach also holds: `_target` (lines 282-287) builds the three-part form as `'_'.join(columns)`, and `_plan_constraint_composite` funnels UNIQUE constraints through the same name, so a UNIQUE over `(a_b, c)` stamps `unique=True` on whichever tuple claimed `a_b_c` first.

The only real mitigation is likelihood, not correctness: it needs two multi-column keys on one table whose column names partition the same underscore string (e.g. `(a_b, c)` and `(a, b_c)`) — contrived, but entirely legal in an introspected database. That lowers severity, not validity; the D4 naming scheme is genuinely ambiguous and fails silently, against the module docstring's own law ("nothing is invented and nothing is dropped").

### [high] A single-member compositeColumn with unique=True renders JSON that SqlModelReader always refuses
- Where: `src/genro_sql/modern/reader.py:235` — dimension: cross-phase — skeptic votes: SSS (S=survives)
- Claim: Phase 4 projects `compositeColumn(name="uq", columns="title", unique=True)` as a hash-named single-column UNIQUE constraint, and Phase 6's reader turns every hash-named UNIQUE back into a composite named `"_".join(columns)` — which for one column IS the column name. The user gets `SqlModelReadError: root.schemas.s.tables.t.columns.title: the composite 'title' synthesized for columns title collides with a column of the same name`, so the plan's round-trip law `render(reader(structure)) == structure` fails on a model the grammar accepts and SqlModelValidator approves.
- Evidence: migration.py:238-244 projects any composite with `unique` truthy via `new_constraint_item(schema, table, _names(columns), "UNIQUE")` with no constraint_name, and `structures.new_constraint_item` then sets `constraint_name = constraint_name or hashed_entity_name` (structures.py:271), so constraint_name == entity_name. structure-1.0.json:127-130 accepts UNIQUE with `minItems: 1`, so the boundary StructureValidator lets it through. reader.py:228-236 `_plan_constraint_composite` fires on exactly `constraint_name == name` with no length guard, calling `_need_composite(key, columns, unique=True)`; reader.py:240 computes `name = "_".join(columns)` == "title"; reader.py:166-172 then raises the collision error because "title" is already in `columns`. validators._check_composites (validators.py:146-160) only checks that members exist, so a one-member composite is legal upstream.
  - HOLDS: The finding holds; every link verified in the code. (1) elements.py:237 declares compositeColumn(name, columns, unique=False) with no minimum member count, and validators.py:146-160 _check_composites only verifies members are physical columns — a one-member composite is legal upstream. (2) migration.py:238-244 projects any composite whose `unique` is truthy via new_constraint_item(schema, table, _names(columns), "UNIQUE") with no constraint_name and no length guard; genro_sqlmigration.structures.new_constraint_item (read from the venv source) sets entity_name = hashed_name(...) and constraint_name = constraint_name or hashed_entity_name, so constraint_name == entity_name. (3) reader.py:228-236 _plan_constraint_composite fires on exactly `attributes["constraint_name"] == name` with no length guard and calls _need_composite(key, columns, unique=True); reader.py:237-245 computes name = "_".join(columns), which for one column IS the column name; reader.py:159-172 then raises SqlModelReadError "the composite 'title' synthesized for columns title collides with a column of the same name". reader.py:284-299 _fill_constraints skips the same constraint (elif constraint_name != name), so no alternate path rescues it. Refutation attempts all failed: no min-2 rule exists anywhere; the sibling _plan_relation_composites at reader.py:220 DOES guard with `if len(columns) > 1`, making the missing guard on the constraint path a clear asymmetry/oversight rather than intent; plan.md:213 and :314 state "composite unique=True becomes a UNIQUE constraint" with the "loud error" reserved for genuine D4 collisions, and scope composites nowhere to >=2 members; the only test is the two-column case (tests/test_wf_phase4_renderer.py:176, columns="a,b"), so the one-column case is untested. Mitigating only in severity: a single-member compositeColumn is degenerate authoring (the grammar directs single-column uniqueness to the column's own unique=True), so it is unlikely in practice — but the model is grammar-legal and validator-approved, Phase 4 renders it, and Phase 6 refuses its own output, so the round-trip law does break.
  - HOLDS: Not refuted — every link verified in the code. elements.py:237 accepts compositeColumn(columns="title") with no arity guard and validators.py:146-160 (_check_composites) checks only non-empty and member existence, so the model passes the boundary. migration.py:238-244 projects any unique composite via new_constraint_item(..., "UNIQUE") with no constraint_name, and structures.py:263-272 (genro-sqlmigration) sets entity_name = hashed_entity_name and constraint_name = constraint_name or hashed_entity_name, so constraint_name == the dict key. reader.py:228-236 (_plan_constraint_composite) therefore does not return (not CHECK, constraint_name == name) and calls _need_composite with a one-element list; reader.py:240 computes name = "_".join(["title"]) == "title"; reader.py:166-172 then raises SqlModelReadError for the composite colliding with the column of the same name. No length guard exists on either side (grep for arity checks in src/genro_sql/modern/ hits only reader.py:220/261/278, all relation-side). So render(reader(structure)) fails on a grammar-accepted, validator-approved model. Caveats on severity only: elements.py:66-67 documents that single-column uniqueness is the column attribute rather than a constraint entity, and reader.py:30 frames the composite round-trip as the multi-column (D4) case, so the input is a documented misuse — but nothing enforces the rule, and the natural fixes are a len(members) > 1 check in _check_composites or routing a one-member unique composite to the column attribute in migration._fill_constraints. The failure is a loud path-naming error, not silent corruption, and the explicit-constraint path (constraint_name != entity_name) round-trips correctly.
  - HOLDS: Could not refute — every premise checks out in the actual code. (1) elements.py:237 and validators.py:146-160 accept a one-member compositeColumn (no member-count check anywhere in validators.py; the only len() comparison, line 262, is relation local-vs-target). (2) migration.py:238-244 projects any composite with truthy `unique` via new_constraint_item(..., _names(columns), "UNIQUE") with no constraint_name and no length guard — the docstring at migration.py:24-25 states the intent ("single-column uniqueness stays the column attribute") but nothing enforces it. (3) genro-sqlmigration structures.py:263-274 sets constraint_name = constraint_name or hashed_entity_name and the item is keyed by entity_name (migration.py:244), so key == constraint_name. (4) structure-1.0.json:127-130 gives UNIQUE columns minItems 1, so the boundary StructureValidator passes it. (5) reader.py:228-236 fires _plan_constraint_composite on exactly constraint_name == name with no length guard; line 240 makes name = "_".join(columns) == the column's own name; reader.py:161-172 then raises SqlModelReadError at root.schemas.<s>.tables.<t>.columns.<col> because columns was populated first (158-165). Two facts strengthen it: the sibling _plan_relation_composites (reader.py:217-225) DOES guard with `if len(columns) > 1`, so the omission is an inconsistency rather than a design choice; and genro-sqlmigration/readers/base_reader.py:195-207 demotes every single-column UNIQUE to a column attribute, so Phase 4's renderer is the only producer of this shape — its own output is what Phase 6 must ingest. Only mitigation is severity: the model is degenerate (unique=True belongs on the column) and the plan's Phase 6 gate states the law JSON-first, which introspected JSON never violates.

## Unverified (30 — listed, not judged)

- [high] (pattern) `src/genro_sql/modern/reader.py:263` — SqlModelReader silently discards a foreign key's constraint_name, breaking the round-trip law it declares
  - Read a normalized structure whose FK carries a readable name (any introspected pre-existing database — PostgreSQL names FKs `book_author_id_fkey` — or any human JSON that uses json_producer's documented relation `name`), and the emitted recipe/re-rendered JSON comes back with `constraint_name` = `fk_<md5-8>`. `json_equal(A, render(read(A)))` is False, and the reverse pipeline's whole point (turn an existing database into a maintainable recipe) produces a recipe that renames every foreign key constraint it touches.
- [high] (coverage) `tests/test_wf_phase5_integration.py:80` — Nothing pins the PG db_creation path dormant — a regression creates a stray database outside the test sandbox
  - If the `RecipeNamedPgDatabase` label trick or the pre-creation of the test database regresses, `added_db` fires and the migrator executes `CREATE DATABASE "library"` (phase 5) / `"recipes"` (phase 7) on the developer's local PostgreSQL. The suite still passes — every assertion is about `getChanges()` — and teardown only drops `test_genro_sql_*`, so the stray database is left behind. This already happened once in this workflow (plan.md:256, log/repair-fable.txt:5).
- [high] (coverage) `src/genro_sql/modern/reader.py:295` — SqlModelReader's named multi-column UNIQUE branch is never exercised
  - A live database with a human-named composite UNIQUE (`uq_author_title` — the normal case) goes through `_fill_constraints`'s `elif constraint_name != name` branch, which no test reaches. If that branch breaks, the constraint is silently dropped from the recovered tree: the emitted recipe omits it, and the next migrator run against the real database emits a DROP CONSTRAINT for a constraint the user never asked to remove.
- [high] (coverage) `src/genro_sql/modern/reader.py:267` — `deferrable` without `initially_deferred` is silently upgraded, and no test distinguishes the two
  - A foreign key introspected as DEFERRABLE INITIALLY IMMEDIATE comes back as `deferred=True`, which the renderer projects as `deferrable=True, initially_deferred=True`. The recovered recipe therefore changes the FK's timing semantics, and no test notices — the reader's own contract ("anything it cannot express is a loud error", docstring lines 33-35) is violated silently instead of raising.
- [medium] (correctness) `src/genro_sql/modern/reader.py:226` — Multi-column FK to a table outside the structure raises a bare KeyError on a tuple
  - Feed the reader a structure containing a two-column FK whose `related_schema`/`related_table` is not among the extracted schemas (routine when introspecting one application schema out of a database) and `to_builder()` dies with `KeyError: ('other', 'city')` from a dict lookup, instead of `SqlModelReadError` naming the JSON path. The single-column form of the same JSON produces a clean `SqlModelValidationError` ("relation target table does not exist"), so the failure mode depends on the FK's arity.
- [medium] (correctness) `src/genro_sql/modern/migration.py:202` — Two relations on one column collapse into one FK, and the rule that forbids it is never checked in this pipeline
  - A recipe that puts two relations on the same column — `author_id.relation(to="public.author", foreign_key=True)` and `author_id.relation(to="public.editor", foreign_key=True)` — renders to a structure containing only the second FK. `render()` reports nothing: it calls `SqlModelValidator`, which has no such check, and never calls `SqlBuilder.validate_source`, the only place the "at most one relation per column" rule lives. The same silent overwrite hits two named UNIQUE constraints over identical columns and two indexes over identical columns (e.g. a full one and a partial one with `where`).
- [medium] (correctness) `src/genro_sql/modern/migration.py:164` — A column that declares only sql_type is projected as dtype 'T' and created as text
  - `column(name="geom", sql_type="geometry(Point,4326)")` projects `attributes = {"sql_type": "geometry(Point,4326)", "dtype": "T"}`, and since no writer reads `sql_type` the migrator issues `"geom" text` — the escape hatch is inverted into a text column, with no warning. The grammar documents the opposite precedence.
- [medium] (correctness) `src/genro_sql/modern/migration.py:137` — pkey is projected verbatim, so a space after the comma fails late with a misleading error
  - `table(name="t", pkey="id, code")` — a natural spelling the grammar accepts and the domain validator approves — fails at the end of `render()` with `SqlValidationError: schemas.s.tables.t: pkey column ' code' is not a table column`, pointing at a column name with a leading space rather than at the recipe's formatting. Every other name collection is normalized on the way out; `pkeys` is the exception.
- [medium] (correctness) `src/genro_sql/modern/reader.py:312` — Reader crashes with a raw grammar error on a legal database whose index or constraint is named like a column
  - PostgreSQL index names live in the relation namespace, not the column namespace, so `CREATE INDEX title ON s.t(title)` is legal. Reading that database raises `ValueError: duplicate collection key 'title' among 'index' siblings` from genro-builders — no path, no mention of the offending index, and not the `SqlModelReadError` the module promises for anything it cannot express. Same for a UNIQUE or CHECK constraint whose name matches a column name (`_fill_constraints`, line 291).
- [medium] (cross-phase) `src/genro_sql/modern/migration.py:118` — A SQL name containing a dot silently corrupts the projection: path splitting is the addressing mechanism in two phases, with different guards
  - Nothing validates that an element's `name` is a legal single Bag label, yet Phase 3 and Phase 4 both address nodes by `path.split(".")` positions. A table or column named `"a.b"` (a legal quoted PostgreSQL identifier, and something SqlModelReader will happily import from an introspected database) makes `set_item` create intermediate bags, so the paths gain segments: the validator silently skips the affected columns while the renderer projects them under the WRONG name — no error anywhere, wrong DDL.
- [medium] (cross-phase) `src/genro_sql/modern/validators.py:79` — validate_model() passes and render() dies with IndexError on a root-mounted table/schema/column
  - Because no element declares `parent_tags`, `root.table(name="x")` (a plausible recipe typo — wrong variable, wrong indentation) builds silently and `validate_source()` reports nothing. `SqlModelValidator` then either skips the node or crashes, and `SqlMigrationRenderer.render()` raises a bare `IndexError: list index out of range` instead of naming the misplaced element — and the two phases fail differently on the same document.
- [medium] (pattern) `src/genro_sql/modern/reader.py:235` — A single-column hash-named UNIQUE constraint makes the reader raise a bogus "collides with a column" error
  - Feed the reader a structure produced by its own documented twin — `JsonStructureProducer` over a human JSON with `constraints: [{"type": "UNIQUE", "columns": ["email"]}]` — and `to_builder()` dies with `root.schemas.s.tables.t.columns.email: the composite 'email' synthesized for columns email collides with a column of the same name`, i.e. an ordinary single-column UNIQUE constraint is reported as a D4 composite-name collision.
- [medium] (pattern) `src/genro_sql/modern/reader.py:267` — deferrable and initially_deferred collapse into one `deferred` flag; a DEFERRABLE INITIALLY IMMEDIATE foreign key comes back as INITIALLY DEFERRED
  - Read a structure whose FK has `deferrable: True` without `initially_deferred` (the normal PostgreSQL `DEFERRABLE INITIALLY IMMEDIATE` form, which both upstream producers and the pg reader represent as two independent flags) and the re-rendered JSON gains `initially_deferred: True`: the round-trip comparison fails, and applying the emitted recipe changes the constraint's timing semantics in the database.
- [medium] (pattern) `src/genro_sql/modern/elements.py:131` — table(comment=...) is documented as semantic but is projected into the normalized JSON
  - A developer reading docs/grammar.md sees `table.comment` listed in the `semantic` plane — the plane the plan header says never reaches the normalized JSON — while the renderer writes it into `attributes.comment`. Either the generated reference lies about the one invariant it exists to document, or the renderer leaks the semantic plane; the two cannot both be right.
- [medium] (pattern) `src/genro_sql/modern/grammar_doc.py:201` — The grammar reference's "Cardinality" line reports how an element keys its CHILDREN while reading as the element's own cardinality
  - docs/grammar.md tells the reader that `index`, `constraint`, `column` and `extension` are "ordered children, not name-addressable" when all four are name-keyed by their parent — which is exactly why `t.index(columns=...)` raises `collection key needs attribute 'name'`. The one generated document a developer consults misinforms on the fact this workflow itself tripped over twice (Phase 2 review item 4, the Phase 7 contract defect).
- [medium] (coverage) `src/genro_sql/modern/reader.py:122` — The reader's event_triggers refusal has no test
  - If the `root.event_triggers` guard is removed or its truthiness check drifts, a structure carrying event triggers is accepted, the reader returns a tree that does not contain them, and the emitted recipe looks complete while the event triggers are gone — the exact silent loss the module docstring promises never happens. Feeding that recipe back to the migrator would then propose dropping them.
- [medium] (coverage) `src/genro_sql/modern/emitter.py:90` — Emitter identifier sanitization is tested only on the non-identifier-character branch
  - A schema or table whose SQL name is a Python keyword (`class`, `import`, `return` — ordinary table names in real schemas), or starts with a digit, or collides with an already-emitted variable, exercises branches no test reaches. A regression there emits a module that raises SyntaxError at exec, or one where a table variable is rebound onto another node and the following `.column(...)` call fails — with CI fully green, because the failure only appears when a user emits that particular schema.
- [medium] (coverage) `src/genro_sql/modern/emitter.py:66` — No emitted-module test contains a string literal needing real escaping
  - A comment or CHECK clause containing a double quote or a backslash — routine in PostgreSQL introspection, where check clauses come back with quoted identifiers such as `(("title")::text <> ''::text)` — takes `_literal`'s `repr` fallback, which no test exercises through an emitted module. If the requoting condition regresses, the emitted recipe is syntactically broken or carries a corrupted string, and only the end user sees it.
- [medium] (coverage) `src/genro_sql/modern/migration.py:271` — Renderer's "skip the auto-index over the pkey columns" rule is untested
  - A recipe with a foreign key on its own primary key (the standard 1:1 child table: `child.id` FK to `parent.id`, `pkey="id"`) or with `indexed=True` on a pkey column relies on the `columns == pkey_columns` skip. If it regresses, the rendered JSON gains an index item over the pkey, and the migrator silently creates a redundant duplicate index in every target database — no error anywhere.
- [low] (correctness) `src/genro_sql/modern/emitter.py:126` — emit() on a model with no db node produces a module that cannot be imported
  - `SqlPythonEmitter(SqlBuilder()).emit()` (or any builder whose `main` added no `db`, e.g. one built from a structure that failed earlier) returns source ending at `    def main(self, root):` with no body. Writing it out and importing it raises `IndentationError: expected an indented block`, instead of the emitter refusing an empty tree.
- [low] (correctness) `src/genro_sql/modern/elements.py:81` — A second root.db() silently replaces the first database and everything under it
  - A recipe whose `main` calls `root.db(name="a")` and later `root.db(name="b")` ends up with only `b`: the first db node and every schema, table and column under it disappear from the source tree with no error at build time and nothing reported by `validate_model()`. The author sees a model that silently lost half its content.
- [low] (correctness) `src/genro_sql/modern/validators.py:182` — An index with no columns validates and projects a column-less index item
  - `t.index(name="idx_typo", method="gin")` (columns forgotten, or misspelled away by a `where`-only edit) passes `validate_model()` and renders to an index entity whose `columns` attribute was stripped by `clean_attributes` and whose hash is computed over an empty column list — so two such indexes on the same table share one entity_name and collapse into one. UNIQUE constraints are protected against exactly this; indexes are not.
- [low] (correctness) `src/genro_sql/modern/__init__.py:47` — Any ImportError inside migration.py or reader.py is reported as a missing optional dependency
  - With genro-sqlmigration correctly installed, a typo in an internal import of `reader.py` — or a broken transitive dependency of it — makes `from genro_sql import SqlModelReader` fail with `ImportError: genro-sqlmigration is required for SqlModelReader: install genro-sql[migration]`, sending the reader off to reinstall a package that is already there.
- [low] (cross-phase) `src/genro_sql/modern/builder.py:80` — The excess-relation report rides the missing-tags slot, and the plan's own pattern reference formats it as "is missing"
  - A consumer written against the documented contract of `validate_source()` prints "db.public.recipe.author_id is missing relation: at most one per column" — the opposite of the truth (there are too many, not too few). The consumer is not hypothetical: it is the cookbook file the plan names as Phase 2's pattern reference.
- [low] (cross-phase) `src/genro_sql/modern/validators.py:182` — An index with no columns passes every gate and projects an index over zero columns
  - `t.index(name="ix")` — `columns` is optional in the signature — builds, validates, and renders to a JSON index item whose `columns` is `{}`. Phase 3 checks "UNIQUE requires columns" for constraints but has no equivalent check for indexes, and neither StructureValidator nor the JSON schema rejects an empty index. The migrator is then asked to create an index on nothing.
- [low] (cross-phase) `docs/grammar.md:236` — docs/grammar.md publishes index.name as optional, but the collection key makes it mandatory
  - A developer consulting the generated reference reads `| name | str | None | physical | the index name. |`, writes `t.index(columns="title")`, and gets `ValueError: 'index': collection key needs attribute 'name', which is missing`. Phase 8's generator is faithful — it is the Phase 2 signature that lies, and Phase 8 promoted the lie into the published contract instead of surfacing it.
- [low] (pattern) `src/genro_sql/__init__.py:22` — `from genro_sql import *` raises ImportError when the migration extra is not installed
  - On a base install (no `genro-sql[migration]`), `from genro_sql import *` — and anything doing `hasattr(genro_sql, "SqlModelReader")` or `dir()`-based introspection — fails with `ImportError: genro-sqlmigration is required for SqlMigrationRenderer`, even for code that only wants `SqlBuilder`. The lazy guard's stated purpose (keep the optional dependency optional) does not hold for the star-import path.
- [low] (pattern) `src/genro_sql/modern/validators.py:182` — An index with no columns passes domain validation and renders as a column-less index item
  - `t.index(name="ix")` (no `columns`) validates clean and renders to an index entity whose attributes carry no columns at all; `StructureValidator` accepts it (the hash of an empty column list matches), so the migrator is handed a CREATE INDEX with nothing to index.
- [low] (pattern) `src/genro_sql/modern/__init__.py:45` — The optional-dependency guard rewrites any ImportError raised inside migration.py or reader.py as "install the extra"
  - If `migration.py` or `reader.py` ever fails to import for its own reason — a renamed symbol in genro-sqlmigration, a typo in a local import — the user is told `genro-sqlmigration is required for SqlMigrationRenderer: install genro-sql[migration]` while the package is installed, sending the diagnosis in the wrong direction.
- [low] (coverage) `src/genro_sql/modern/migration.py:164` — The documented dtype default for a column with no dtype is never rendered
  - A recipe column written without `dtype` is documented to render as `A` when it has a `size` and `T` otherwise (elements.py:142-143). No test renders such a column, so a regression here silently ships the wrong SQL type — a text column where a varchar was meant, or vice versa — with no diagnostic.
